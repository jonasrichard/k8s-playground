apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: headlamp-http-route
  namespace: kube-system
spec:
  parentRefs:
    - name: eg
      namespace: default
  hostnames:
    - "headlamp.localhost"
  rules:
    - backendRefs:
        - name: my-headlamp
          port: 80